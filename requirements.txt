# Python dependencies for scripts/ that run outside FreeCAD's bundled
# interpreter (plain `python3`, not `run_freecad_script.sh`).
#
# FreeCAD itself is not pip-installable; see cad/README.md for how to get it.
# CadQuery (used by scripts/export_so101_wrist.py and cad/cadquery/*) is
# pinned separately in cad/cadquery/requirements.txt for its own environment;
# set CADQUERY_PYTHON to that environment's interpreter when exporting.
numpy
pytest
shapely
trimesh
vtk
